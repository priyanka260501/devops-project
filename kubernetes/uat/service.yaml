apiVersion: v1
kind: Service
metadata:
  name: myapp-service
  namespace: uat
  labels:
    app: myapp
    environment: uat
spec:
  type: NodePort
  selector:
    app: myapp
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30084
