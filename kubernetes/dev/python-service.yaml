apiVersion: v1
kind: Service
metadata:
  name: python-service
  namespace: dev
  labels:
    app: python-app
    environment: dev
spec:
  type: NodePort
  selector:
    app: python-app
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
    nodePort: 30083
